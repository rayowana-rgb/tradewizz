# 2026-06-19 (Fri, GMT+8)

## Screener engine load — Lapis 1: single-flight lock (CLOSED only) [00:45]
- User concern: banyak user screening -> engine kelelahan. Clarified: engine sudah di-cache per (market, cache_key); OPEN never runs engine (except non-saved cold-open); reuse across users works. Real gaps: (1) thundering herd (no lock) -> N concurrent same-key requests = N engine runs; (2) cold-open not saved; (3) cache-key explosion from limit/min_score.
- Lapis 1 (requested, CLOSED-only): module-level shared lock registry _RUN_LOCKS keyed by (market_value, cache_key) + _RUN_LOCKS_GUARD, helper _run_lock_for(). Service is constructed per-request in main.py so lock MUST be module-level. _run_and_save now: acquire per-key lock -> double-checked re-read store (get_for_date, skip if data newer/written-after) -> reuse if present, else run engine once + save. force_refresh=True bypasses reuse but still holds lock (no parallel forced runs). Only CLOSED path runs engine, so guard only there.
- Tests: +2 (test_single_flight_collapses_concurrent_closed_runs: 12 threads, blocking run_screen -> calls==1, save_count==1; test_single_flight_parallel_keys_run_independently: diff keys -> 2 runs). 21 screener-cache tests pass; full backend 858 passed (was 856). Restarted uvicorn, /v1/health 200.
- NOT done yet (proposed, deferred): Lapis 2 (warmer pre-runs engine for default params + saves snapshot) and Lapis 3 (normalize cache-key: compute at MAX_LIMIT/min_score=0 superset, filter in-memory).

## Screener engine load — Lapis 2: pre-warm default snapshot [01:10]
- Goal: user requests for DEFAULT params should never trigger cold-start engine run. Hook into warmer: after each market's OHLCV cache is warmed, pre-compute + persist the default screener snapshot.
- warmer.py: DailyCacheWarmer now takes optional on_warmed(market, trading_date) callback; fired in tick() AFTER _warm_market + archive purge, wrapped in try/except (never breaks warm loop). last_warm[market] gains "prewarmed_screener": True/False/None.
- main.py: _prewarm_default_screener(market, trading_date) builds ScreenerCacheService with default key (category="", limit=DEFAULT_LIMIT=50, min_score=0, min_value_traded=default_min_value_traded(market)) and calls .get(force_refresh=True) -> runs engine once + saves. Wired as on_warmed= on the module _cache_warmer. Uses _screener_now_override now_provider (live clock = CLOSED post-warm, so force_refresh allowed).
- Tests +4: hook fires w/ args + prewarmed_screener True; hook error swallowed -> warm still ok + flag False; no hook -> None; INTEGRATION: pre-warm runs engine once then first default /screen request is cached hit (engine_calls stays 1). Backend 862 pass (was 858). Restarted uvicorn /v1/health 200, warmer enabled.
- Remaining (Lapis 3, deferred): normalize cache-key (compute MAX_LIMIT/min_score=0 superset, filter limit/min_score in-memory) to collapse user param variants into fewer engine runs.

## Screener engine load — Lapis 3: cache-key normalization (superset) [01:40]
- Goal: variasi limit/min_score user jangan melipatgandakan engine run. Cache engine di SUPERSET (limit=MAX_LIMIT=200, min_score=0), apply limit/min_score user di memori saat serve.
- Confined to main.py (KEEP make_cache_key + ScreenerCacheService semantics intact). New helper _slice_screener_result(result, limit, min_score): filter m.score>=min_score (Base Score, per engine semantics), truncate to limit, recompute total_count(before limit)/returned_count/limit/min_score; preserve cache/market metadata via model_copy(deep=True).
- /screen route: cache_key + _run now use limit=MAX_LIMIT, min_score=0 (only category + min_value_traded vary the heavy run). Tier cap_screener_limit still folded into user `limit` -> applied in slice (caps what user sees, not the run). _prewarm_default_screener (Lapis 2) updated to same superset key so prewarm matches what /screen reads.
- Tests: new tests/test_screener_cache_normalize.py (OFFLINE, fake counting engine + InMemory store + neutralized freshness probes via functools.partial(ScreenerCacheService, latest_data_timestamp=None-ish, latest_write_timestamp=None) monkeypatched onto main.ScreenerCacheService): varied limit(5/50/200)+min_score collapse to calls.n==1, engine invoked at [200]/[0.0], each response sliced correctly; min_score slice faithful. 859 offline pass.
- NOTE: tests/test_screener_cache_api.py is LIVE-NETWORK (real Yahoo screen per market) -> very slow / rate-limited; not a reliable gate. Use offline suite + new normalize test instead.
- LIVE VERIFY (api restarted, /v1/health 200): /screen/IDX limit=3 -> cached total=200 returned=3; limit=10 -> still cached=true (reused same superset run); limit=10&min_score=80 -> 3 rows all>=80. All 3 layers (1 single-flight, 2 prewarm, 3 normalize) live.

## BUG: Explore screening showed stale/mock data — ROOT CAUSE + FIX [04:30]
- Report: hasil screening di Explore "bukan data close terbaru".
- Investigasi: backend SEBENARNYA punya snapshot benar (semua market market_date=2026-06-18 = close terbaru; Jumat 19 pagi pasar belum tutup). Data tidak basi.
- ROOT CAUSE: app fallback ke MOCK. api_client._get on TimeoutException -> _maybeFallback -> _mockScreen (app requestTimeout=25s). Yang memicu timeout: setelah Lapis 3, cache-key MASIH menyertakan `category`, jadi tiap kategori (bullish/scalping/dst) = engine run TERPISAH & penuh saat pertama dibuka di Explore. Run kategori cold bisa >25s (>60-94s) -> app timeout -> tampil mock ("bukan close terbaru"). Padahal engine sudah menghitung SEMUA kategori dalam 1 pass; filter kategori hanya post-filter (wanted.intersection(m.categories) di engine.py:1290).
- DB bukti: IDX 2026-06-18 hanya punya snapshot _all + bullish; momentum/scalping/ara_hunter dll TIDAK ada -> cold run tiap dibuka.
- FIX (Lapis 3.5, app/main.py /screen route): keluarkan category dari cache_key & engine _run (selalu superset _all, categories=None, limit=MAX_LIMIT, min_score=0); filter kategori (+ min_score + limit) di _slice_screener_result di memori. Semua kategori + param variant collapse ke 1 cached superset per (market, min_value_traded floor). Hapus _category_cache_token (dead). Lapis 2 prewarm sudah pakai category="" -> tetap konsisten.
- TEST: tests/test_screener_cache_normalize.py +1 (test_category_filter_slices_the_shared_all_superset): kategori TIDAK trigger run kedua (calls.n==1), bullish 10/20 sliced. 860 offline pass.
- LIVE VERIFY (restart, /v1/health 200): /screen/IDX semua kategori (momentum/bullish/scalping/ara_hunter/pullback/short_candidate) -> cached=True, ~1.5s, market_date=2026-06-18, total beda per kategori (bullish 22, scalping 166). Tidak ada lagi cold per-category -> tidak ada timeout -> tidak ada mock fallback.
- CATATAN: 'momentum' bukan ScreenerCategory enum (hero tag) -> _parse_categories drop -> no filter (return _all). Kategori valid: bullish,bearish,scalping,accumulation,pullback,accumulation_silent,turnaround_multibagger,frequently_traded,short_candidate,ara_hunter.
