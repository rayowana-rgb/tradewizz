"""Global Rotation Engine (Phase 3).

Compares all supported markets and ranks them by opportunity environment, using
the EXISTING Radar pipeline (screener + ranking + regime). Produces a per-market
rotation score and an OVERWEIGHT / NEUTRAL / UNDERWEIGHT / AVOID recommendation.
No LLM, no new scoring, no broker contact.
"""
